//! The text render buffer built on top of cursive
//! 
//! It handles a few things differently like overriding input handling to 
//! manually scroll and manual text highlighting for certain words
//! 
//! The buffer is initialised with a GitBuffer object which represents the current state 
//! of a git repository
//! 
//! ```notest
//! let b = Buffer::new(git_buffer);
//! ```


pub struct Buffer {

}

impl Buffer {

}